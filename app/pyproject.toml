[tool.pytest.ini_options]
testpaths = ["tests"]
# Add app/ to sys.path so tests can import handler directly
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
