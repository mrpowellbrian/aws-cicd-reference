# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
tfplan.json
override.tf
override.tf.json
crash.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
.pytest_cache/
.coverage
.ruff_cache/

# Lambda deployment package — generated artifact
*.zip

# Local Terraform backend config — contains bucket name, never commit
infra/backend.hcl

# Secrets — never commit
.env
*.pem

# OS
.DS_Store
Thumbs.db
