name: PR validation

on:
  pull_request:
    branches: [main]

# Deny all permissions at workflow level; grant minimally per job.
permissions:
  contents: read

jobs:
  lint-test:
    name: Lint and test
    uses: ./.github/workflows/_lint-test.yml
    permissions:
      contents: read
